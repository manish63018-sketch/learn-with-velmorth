<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network: Required for Google Fonts loading and future cloud sync -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Notifications (API 33+): Streak reminders and goal nudges — runtime-requested -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Microphone: AI Speaker screen only — runtime-requested, never pre-prompted -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- WorkManager: Allows streak check job to fire after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
        ❌ REMOVED: CAMERA (not needed — no photo capture feature)
        ❌ REMOVED: READ_MEDIA_IMAGES/VIDEO/AUDIO (not needed — content is bundled)
        ❌ REMOVED: READ/WRITE_EXTERNAL_STORAGE (not needed — Room DB is internal)
        Rule: Only declare permissions that are ACTIVELY used by a current V1 feature.
    -->

    <application
        android:name=".VelmorthApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:windowSoftInputMode="adjustResize"
        android:theme="@style/Theme.LearnWithVelmorth">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>

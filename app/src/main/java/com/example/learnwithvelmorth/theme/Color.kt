package com.example.learnwithvelmorth.theme

import androidx.compose.ui.graphics.Color

// === Forest Primary Palette ===
val ForestDeep = Color(0xFF1B4332)       // Deep forest green — primary brand
val ForestMid = Color(0xFF2D6A4F)        // Forest mid-tone
val MossGreen = Color(0xFF40916C)        // Moss — primary variant
val LeafGreen = Color(0xFF52B788)        // Bright leaf
val MintGreen = Color(0xFF74C69D)        // Mint accent
val PaleGreen = Color(0xFFB7E4C7)        // Pale green tint
val FogGreen = Color(0xFFD8F3DC)         // Very light green surface

// === Warm Neutrals ===
val CreamWhite = Color(0xFFF1E8D0)       // Cream — surface / background
val WarmWhite = Color(0xFFFAFAF5)        // Off-white — card surfaces
val SoftBeige = Color(0xFFEDE0C8)        // Beige — secondary surface
val BarkBrown = Color(0xFF8B6914)        // Dark bark — for borders
val EarthBrown = Color(0xFF6B4226)       // Deep earth brown

// === Leaf Gold (Currency / Premium) ===
val LeafGold = Color(0xFFD4A017)         // Leaf gold — currency color
val LeafGoldLight = Color(0xFFEFC050)    // Light gold — highlights
val LeafGoldDark = Color(0xFFA87C10)     // Dark gold — shadows

// === Accent / Feedback ===
val SunriseOrange = Color(0xFFE76F51)    // Warm orange — streak / fire
val CoralRed = Color(0xFFD62828)         // Error / wrong answer
val SkyBlue = Color(0xFF457B9D)          // AI Speaker accent
val LavenderMist = Color(0xFF9B89C4)     // Premium accent

// === Dark Theme Variants ===
val ForestNight = Color(0xFF0D2418)      // Very dark green — dark BG
val MossNight = Color(0xFF1A3A28)        // Dark surface
val LeafNight = Color(0xFF2D6A4F)        // Dark primary

// === Text ===
val TextDark = Color(0xFF1C2B1E)         // Near-black green-tinted text
val TextMid = Color(0xFF4A5C4E)          // Mid grey-green text
val TextLight = Color(0xFF8FA896)        // Muted text

// === White / Black ===
val PureWhite = Color(0xFFFFFFFF)
val PureBlack = Color(0xFF000000)
